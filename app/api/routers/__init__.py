"""Routers da API (Ordem 4).

Pacote regular, com `__init__.py`, para acompanhar a convenção do resto de
`app/` — sem ele, `app/api/routers` vira namespace package e o mesmo arquivo
passa a ser resolvido por dois nomes de módulo diferentes.
"""
